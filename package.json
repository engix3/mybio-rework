{
  "name": "mybio-rework",
  "version": "1.0.0",
  "private": true,
  "description": "Personal bio link site with lightweight quality checks.",
  "type": "module",
  "scripts": {
    "check:syntax": "node --check script.js && node --check system.js && node --check api/deezer.js",
    "lint": "eslint .",
    "test": "node --test tests/**/*.test.mjs",
    "quality": "npm run check:syntax && npm run lint && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0"
  }
}
